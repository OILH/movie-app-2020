import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import Movie from './Movie';

ReactDOM.render(<App />, document.getElementById('root'));  // React App은 하나의 component만을 rendering해야 함. 여기에서는 App
